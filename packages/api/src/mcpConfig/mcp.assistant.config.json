{
  "mcpServers": {
    "postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "{{PG_ASSISTANT_CONNECTION_STRING}}"
      ]
    }
  }
}
